<UserControl x:Class="DriverSolution.customControl.MenuButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DriverSolution.customControl"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300" x:Name="this" Background="White">
   
    <UserControl.Resources>
 <!--       Setting default Icon if we didn't bind any icon in MainWindow-->
 
        <PathGeometry x:Key="DefaultIcon" Figures=""></PathGeometry>
        <Style TargetType="{x:Type ToggleButton}">
            <Setter Property="Height" Value="48"></Setter>
            <Setter Property="HorizontalAlignment" Value="Stretch"></Setter>
            <Setter Property="HorizontalContentAlignment" Value="Center"></Setter>
            <Setter Property="Background" Value="Transparent"></Setter>
            <Setter Property="BorderThickness" Value="0"></Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                    <ColumnDefinition Width="*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <Path Data="{Binding Icon, ElementName=this, FallbackValue=DefaultIcon, TargetNullValue=DefaultIcon}"
                                      HorizontalAlignment="Center" Stretch="Uniform" Height="17" Width="18" Fill="#515EC1">
                                </Path>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>
    </UserControl.Resources>
    <ToggleButton></ToggleButton>
</UserControl>
